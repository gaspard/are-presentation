import { Simulator } from "./components/Simulator";

# Simulator

On affiche la position (axe orange) et la vitesse (axe vert) d'un ressort:

$$
\ddot{x} = -\frac{k}{m} x
$$

Avec et sans Runge-Kutta:

<Simulator/>